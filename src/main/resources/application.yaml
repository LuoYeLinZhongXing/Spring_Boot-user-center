spring:
  application:
    name: user-center
  datasource:
    url: jdbc:mysql://localhost:3306/louye?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 040810
    driver-class-name: com.mysql.cj.jdbc.Driver
  session:
    timeout: 86400 #session超时时间，默认30分钟

mybatis-plus:
  configuration:
    mapUnderscoreToCamelCase: true
  global-config:
    db-config:
      logic-delete-field: isDelete
      logic-delete-value: 1
      logic-not-delete-value: 0

server:
  port: 8080
